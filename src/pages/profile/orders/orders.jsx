import React from 'react';
import InfoBlock from "@components/info-block/info-block";

const ProfileOrders = () => {
	return (
		<div>
			<InfoBlock title="No access" message="Страница временно не доступна"/>
		</div>
	);
};

export default ProfileOrders;
